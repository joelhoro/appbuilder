<Window x:Class="AppRunner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:AppRunner.Controls"
        Title="MainWindow" Height="708" Width="1255.667">
    <Grid>
        <StackPanel Margin="0,0,0.333,9.667">
            <StackPanel Orientation="Horizontal"  Height="537">
                <controls:DirectoryListControl x:Name="dirlist" HorizontalAlignment="Left" Margin="10,20,0,-110.667" VerticalAlignment="Top" Height="628" Width="439"  />
                <controls:LogFileControl x:Name="logfilecontrol1" Height="650" Width="770" />
            </StackPanel>

            <!--<TabControl Height="600">
                <TabItem Header="Hindsight">
                    <Grid Background="#FFE5E5E5" Margin="0,3,17.333,-163">
                        <StackPanel>
                            <controls:ApplicationControl x:Name="appcontrol1" Margin="27,10,0,0"/>
                        </StackPanel>

                    </Grid>
                </TabItem>
                <TabItem Header="Insight">
                    <Grid Background="#FFE5E5E5">
                        <controls:ApplicationControl x:Name="appcontrol2" />
                    </Grid>
                </TabItem>
            </TabControl>-->
        </StackPanel>
        <!--<myControl:UserControl1 x:Name="mycontrol" ></myControl:UserControl1>-->
    </Grid>
</Window>

<UserControl x:Class="AppRunner.Controls.LogFileControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Width="400">
    <Grid Margin="0,0,-236,0" >
        <StackPanel Orientation="Vertical" >
            <StackPanel Orientation="Horizontal">
                <Label Content="{Binding ActiveApplication.Description}" FontWeight="Bold" FontSize="14" />
                <Label Content="{Binding ActiveApplication.Status}"></Label>
            </StackPanel>
            <TextBox Name="MainTextBox" TextWrapping="Wrap" AcceptsReturn="True"  ScrollViewer.VerticalScrollBarVisibility="Visible" Margin="7,0,10.2,0" Height="433" FontFamily="Consolas" />
        </StackPanel>
    </Grid>
</UserControl>

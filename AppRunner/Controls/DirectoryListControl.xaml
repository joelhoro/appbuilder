<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Models="clr-namespace:AppRunner.Models" x:Class="AppRunner.Controls.DirectoryListControl" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListBox x:Name="listbox" ItemsSource="{Binding Files}" DisplayMemberPath="LogFileName"  SelectionChanged="SelectionChange">
            <!--<ListBoxItem Content="{Binding LogFileName}"></ListBoxItem>-->
        </ListBox>
    </Grid>
</UserControl>

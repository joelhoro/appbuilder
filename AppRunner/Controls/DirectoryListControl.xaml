
    <UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Models="clr-namespace:AppRunner.Models" x:Class="AppRunner.Controls.DirectoryListControl" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
        
    <Grid>
        <StackPanel>
            <Button Content="Refresh" Click="Button_Click"></Button>
            <ListBox x:Name="listbox" ItemsSource="{Binding Files}" DisplayMemberPath="FileName"></ListBox>
            <DataGrid x:Name="fileGrid" ItemsSource="{Binding Files}" AutoGenerateColumns="False" CanUserSortColumns="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="FileName" Binding="{Binding FileName}" />
                    <DataGridTextColumn Header="Creation Date" Binding="{Binding CreationDate}" />
                    <DataGridTextColumn Header="FileSize" Binding="{Binding FileSize}" />
                </DataGrid.Columns>
                <DataGrid.GroupStyle>
                    
                </DataGrid.GroupStyle>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
    
